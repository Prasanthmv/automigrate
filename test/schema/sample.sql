create table person (id int primary key, full_name text);

create table person (age int);

create table person (id int primary key, full_name text, age int);

create table person (id int primary key, full_name text, age int, email varchar);

create table person_address (id int primary key, pid int , street_1 varchar, street_2 varchar);
